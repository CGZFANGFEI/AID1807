什么是事件
    所有的GUI编程，对图形的控制都是通过事件来完成的，简单一点解释，我们没按下一个按钮所做的操作就是事件。

事件三要素：
    事件来源(source)
        状态发生变化的对象，这个变化的对象会产生事件
    事件对象(Object)
        产生的事件是什么
    事件目标(aim)
        这个事件希望被通知的对象

使用事件：
    信号、槽机制：
    信号和槽是用来进行对象间沟通的。当一个特定的事件发生时一个信号被激发。槽用来接收信号，可以使任意的Python请求。当和槽相关联的信号被激发时，槽被调用。
